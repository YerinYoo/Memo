프로젝트 >> 이력서, 자소서(임팩트 있는 말 먼저 > 비전공자가 이 분야를 시작한 이유 등), 포트폴리오

포트폴리오 : ppt X > URL로 첨부 (첫 화면에 users, admin 버튼 하나 씩 > 누르면 페이지 구현되도록)

스레드 : 사용자가 회원 가입을 하였을 때, 회원 정보가 DB에 저`장됨과 동시에 웰컴 메일을 보낼 때 이용되는 기능

//JavaScript vs. JQuery 
속도 JS가 더 빠름 (사람이 실제로 차이를 체감하기는 힘듦)
사용하기에는 JQ가 더 편함, 사용 빈도 더 높음

//JavaScript, JQuery : 웹 개발 시 무조건 알아야하는 필수 문법
* Validation Check >> JS, JQ로 함 (회원가입 시 문항 하나하나 Validation Check를 거치게 됨)
- 원자성
- 적합성 : 데이터가 들어가는 곳에 입력된 데이터가 적합한지 ex. 주민등록번호 창 : 공일공오이오 ... (숫자 아닌 한글로 입력 >> 부적합)

ex. 로그인 시, 입력 없이 로그인 버튼 눌렀을 때 발생하는 에러 창을 띄워주는 언어
ex. 회원가입 시, 아이디에 한글이 입력되어 에러가 났을 때 처리하는 언어

* DOM : XML, HTML 등 문서의 각 항목을 계층으로 표현하여 생성, 변형, 삭제할 수 있도록 돕는 인터페이스
>> HTML 파일 그대로를 카피하여 보이지 않는 가상 현실에 존재하는 파일, DOM 수정 시 원본 HTML 파일에도 수정 발생

* GIT : 소스를 관리하는 방법론, 소프트웨어 >> 방법론을 파일로 관리하는 형태
* GIT Hub : GIT 방식을 통해 소스 관리를 하는 공간 >> 형상 관리를 해주는 클라우드 서비스 공간의 개념
: Repository : Add (working directory) >> Commit (staging area)>> Push (localrepo) >> GITHub (remote)

* 병합, 충돌, merge, fork

//JAVA
* JAVA class :  객체를 정의하는 틀 또는 설계도 *******매우 중요
* 객체 (object) : 물리적으로 존재하거나 추상적으로 생각할 수 있는 것 중 자신의 속성을 가지고 있고 식별 가능한 것
		     : 클래스의 인스턴스나 배열 >> 변수, 자료 구조, 함수, 메서드, 식별자에 의해 참조된 메모리 상의 값 등
* 인스턴스 :  클래스를 바탕으로 실체화되어 메모리에 할당된 실체
+) 객체와의 차이점 
- 객체 : 소프트웨어 상에 구현할 대상
- 인스턴스 : 설계도 (클래스)에 의해 소프트웨어 상에 구현된 실체

*상속 - 부모 (상위) 클래스, 자식 (하위) 클래스
+) 자식 클래스가 부모 클래스를 상속 받지 못하는 경우
- 부모 클래스의 private 접근 제한을 갖는 필드 및 메소드는 자식이 물려받을 수 없음
- 부모와 자식 클래스가 서로 다른 패키지에 있다면, 부모의 default 접근 제한을 갖는 필드 및 메소드도 자식이 물려받을 수 없음
(default 접근 제한은 ‘같은 패키지에 있는 클래스’만 접근이 가능하게끔 하는 접근 제한자이기 때문입니다.)

//SQL
: select, delete, insert, update ...

* JOIN *****매우 중요
: 서로 다른 테이블의 내용을 한 번에 표기할 때 사용하는 구문

1. Inner Join
2. Outer Join
- Left Join
- Right Join
- Full Join

* 데이터 베이스 순서
- Modeling : 정규화
- DML (테이블 생성, 삭제) 
- DDL(insert, update, delete, join ...) 
- Advance (view, function, index, frigger ...)
+) index & cardinality 함께 연계하여 개념 공부하기

* Modeling

* Sequence : 오라클 외의 다른 DB에는 기본 테이블에 탑재되어 있음

* 아마존
- RDS : DB (MySQL)
- EC2 : 컴퓨터 한대를 빌리는 개념 (RDS의 DB와 통신)
- S3 : 컴퓨터의 일부 공간을 빌려 파일을 주로 업로드

--------------------------------------------------------------------------------------------------------------

//BootStrap

//JAVA

//DB Modeling

//Spring

//JavaScript & JQuery

##단축키
1. 윈도우 + D : 바탕화면으로 돌아가기
2. 윈도우 + E : 탐색기 열기
3. ctrl + F4 : 창 닫기
4. ctrl + shift + B : 크롬 북마크 바 숨기기
5. ctrl + U : 화면 소스 보기 
6. ctrl + T : 인터넷 탭 새로 열기
7. F12 : 개발자 툴 열기(elements, console 등)
8. alt + tap : 창 넘기기

